# System Architecture Content Navigation Redesign Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the system architecture interest track discoverable, visually understandable as a complete learning path, readable for experienced developers, and navigable from every page.

**Architecture:** Keep the existing Markdown content model and linear route registry unchanged. Add the interest track to shared/static home navigation, make existing route-index pages carry the learning map and prerequisite/next-step explanations, and give each formal unit a consistent Chinese-first orientation block and cross-links. Extend the course checker so the new learner-facing contract cannot silently regress.

**Tech Stack:** Markdown with YAML frontmatter, Node.js, gray-matter, marked, static HTML generated by website/scripts/build-site.js, custom Node.js checks under website/scripts/.

**Spec:** docs/superpowers/specs/2026-09-03-system-architecture-content-navigation-design.md

## Global Constraints

- Work only in /Users/johnny/Development/vibe-coding/ideas/education/gulou-core/.worktrees/system-architecture-learning-path on branch docs/system-architecture-learning-path.
- Do not modify gulou-agent/, the育儿主线, website/scripts/route-registry.js, the authoring guide, the source matrix, or the validation protocol.
- Preserve the existing route registry frontmatter contract, ten-stage order, six-formal-unit exact set, and four-layer formal-unit headings.
- Use Chinese-first learner copy: first occurrence is 中文名称（English Name）; preserve official API/config/protocol/product names with an explanation.
- Do not add new Markdown files below interests/system-architecture/; all maps, glossary entries, and navigation remain in existing route-index and formal-unit files.
- Preserve unrelated untracked derby.log and logs/ files; never stage or delete them.
- Use apply_patch for file edits and run the smallest relevant check after each task.

---

### Task 1: Encode the new learner-facing content contract

**Files:**
- Modify: website/scripts/check-system-architecture.js:7-197
- Test: website/scripts/check-system-architecture.js through npm run check:system-architecture

**Interfaces:**
- Consumes: existing readMarkdown, validateHeadingsInOrder, PHASES, and REQUIRED_SLICE.
- Produces: root, phase, and formal-unit content-marker assertions used by later tasks.

- [ ] **Step 1: Add failing root and phase/unit marker assertions**

Add these constants after REQUIRED_PROTOCOL_HEADINGS:

~~~
const REQUIRED_ROOT_MARKERS = [
  '## 这条路线解决什么问题',
  '## 一张图看懂路线',
  '## 两种阅读方式',
  '## 四层学习法',
  '## 如何判断完成',
];
const REQUIRED_PHASE_HEADINGS = [
  '## 阶段目标',
  '## 进入条件',
  '## 这一阶段先把什么讲清楚',
  '## 组件如何承载这些思想',
  '## 综合项目产物',
  '## 阶段挑战',
  '## 阅读顺序',
];
const REQUIRED_FORMAL_ORIENTATION_HEADINGS = [
  '## 一句话理解',
  '## 本页要解决的问题',
  '## 本页词汇',
  '## 在路线中的位置',
  '## 下一步',
];
~~~

Add this helper after validateStringArrayField:

~~~
function validateSourceContains(content, markers, rel) {
  for (const marker of markers) {
    if (!content.includes(marker)) fail(rel + ': 缺少 ' + marker);
  }
}
~~~

Call validateSourceContains on the root content with REQUIRED_ROOT_MARKERS. Replace the phase loop's six-heading block with validateSourceContains(page.content, REQUIRED_PHASE_HEADINGS, rel). After the existing formal-unit heading-order check, call validateSourceContains(page.content, REQUIRED_FORMAL_ORIENTATION_HEADINGS, rel).

- [ ] **Step 2: Run the checker and record the expected failure**

Run:

~~~
cd website
npm run check:system-architecture
~~~

Expected result: fail on the first missing root marker because the current root index has no new learner-map heading. Do not weaken the assertions to make the old content pass.

- [ ] **Step 3: Add static homepage entrance assertions**

Read website/scripts/build-site.js and assert that it contains the shared interest link and the dynamic interest section:

~~~
const siteBuilder = fs.readFileSync(path.join(ROOT, 'website/scripts/build-site.js'), 'utf8');
validateSourceContains(siteBuilder, ["siteUrl('interests/')", '按兴趣探索'], 'website/scripts/build-site.js');
~~~

Keep the existing interest-index link assertion.

- [ ] **Step 4: Run focused checks and commit the contract**

Run npm run check:system-architecture and git diff --check. The checker may still fail because the root, phase pages, and formal units are not updated. Commit only the checker change:

~~~
git add website/scripts/check-system-architecture.js
git commit -m "test(architecture): Define learner navigation contract"
~~~

### Task 2: Make the interest track visible from every site entry

**Files:**
- Modify: website/scripts/build-site.js:229-250 and 511-529
- Test: generated website/site/index.html and one generated content page after npm run build

**Interfaces:**
- Consumes: registry.byRel, resolvePath, and siteUrl.
- Produces: collectInterestRouteIndexes(registry) and renderInterestCards(registry); the generated homepage and every content page expose the interest track.

- [ ] **Step 1: Extend the failing builder assertion**

Require the builder source to contain collectInterestRouteIndexes, renderInterestCards, and the visible system architecture label. Run npm run check:system-architecture and confirm it fails before implementation.

- [ ] **Step 2: Add the shared top-nav link**

In renderTopNav(), insert the following after the stages link:

~~~
Use the existing template expression so the generated href is siteUrl('interests/') and respects BASE_PATH:

~~~
<a href="${siteUrl('interests/')}">兴趣副线</a>
~~~
~~~

Keep the existing references, contribution, and GitHub links.

- [ ] **Step 3: Add registry-backed interest-card helpers**

Add these functions immediately before renderHomePageWithRoutes:

~~~
function collectInterestRouteIndexes(registry) {
  return Array.from(registry.byRel.values())
    .filter(page => page.rel.startsWith('interests/')
      && page.rel.endsWith('/_index.md')
      && page.fm.page_type === 'route-index')
    .sort((a, b) => {
      if (a.rel === 'interests/system-architecture/_index.md') return -1;
      if (b.rel === 'interests/system-architecture/_index.md') return 1;
      return String(a.fm.name || a.rel).localeCompare(String(b.fm.name || b.rel));
    });
}

function renderInterestCards(registry) {
  const cards = collectInterestRouteIndexes(registry).map(page => {
    const slug = resolvePath(page.rel);
    const name = page.fm.name || page.fm.topic || page.rel;
    const description = page.fm.description || '跨阶段兴趣学习路径';
    return '<a href="' + siteUrl(slug) + '" class="stage-card">'
      + '<h3>' + name + '</h3>'
      + '<p class="age">' + description + '</p>'
      + '</a>';
  }).join('\\n');
  return '<section class="home-section">'
    + '<h2>按兴趣探索</h2>'
    + '<p>兴趣副线把一个主题从入门连接到进阶和生产实践；选择一条路线后，从它的总入口开始。</p>'
    + '<div class="stage-grid entry-grid">' + cards + '</div>'
    + '</section>';
}
~~~

- [ ] **Step 4: Insert the cards into the dynamic homepage and verify the two-click path**

After the dynamic homepage stage section and before the contribution section, interpolate renderInterestCards(registry). Run:

~~~
cd website
npm run build
rg -n "兴趣副线|按兴趣探索|系统架构与 AI 工程" site/index.html site/interests/index.html site/interests/system-architecture/index.html
~~~

Expected: the home page has the interest link, the interest section, and a first card linking to the system architecture course; an article page also has the top-nav interest link.

- [ ] **Step 5: Run focused checks and commit**

Run npm run check:system-architecture, npm run check:routes, and git diff --check. Commit:

~~~
git add website/scripts/build-site.js
git commit -m "feat(site): Expose interest learning tracks"
~~~

### Task 3: Rebuild the interest index and course map

**Files:**
- Modify: interests/_index.md
- Modify: interests/system-architecture/_index.md
- Test: npm run check:system-architecture and generated course-index inspection

**Interfaces:**
- Consumes: the homepage card target from Task 2 and existing phase/formal-unit Markdown links.
- Produces: a reader-facing interest landing page and a course root whose first screen explains the three macro arcs, main route, vertical slice, four-layer method, and completion criteria.

- [ ] **Step 1: Add a featured entry to the interest index**

Place before 已收录兴趣领域:

~~~
## 从一条完整路线开始

如果你想系统理解一个主题，而不是只找一篇零散文章，可以从[系统架构与 AI 工程](system-architecture/_index.md)开始。它面向已有多年开发经验的工程师，沿着“底层思想 → 组件落地 → 生产案例 → 动手验证”贯通计算机系统、网络、分布式系统、数据、云原生和 AI 工程。

先看[路线地图](system-architecture/_index.md)，再选择完整主干或第一条日志、状态与恢复纵向切片。页面中的英文只保留协议、API 和组件的可检索原名，概念解释以中文为主。
~~~

Rewrite the later directory-structure paragraph so learners begin at each interest's _index.md; keep contribution instructions after the domain table.

- [ ] **Step 2: Replace the course root opening with the approved map**

Keep frontmatter. Use this exact heading order and link targets:

~~~
# 系统架构与 AI 工程

## 这条路线解决什么问题

当一个服务从单机发展到多副本、实时数据链路和 AI 能力时，真正困难的不是背组件名，而是解释状态在哪里、确认意味着什么、故障如何传播、数据怎样恢复，以及为什么一个方案在新约束下仍然成立。本路线把这些判断拆成十个阶段，并用同一个 AI 内容推荐平台作为练习载体。

## 一张图看懂路线

| 分区 | 阶段 | 这一段要回答的问题 |
|---|---|---|
| 基础与方法 | [0 架构方法](00-architecture-method/_index.md) → [1 计算机系统](01-computer-systems/_index.md) → [2 网络](02-network/_index.md) | 如何从需求和约束开始，解释单机行为，再判断远程调用的延迟、超时和部分失败？ |
| 分布式与数据 | [3 分布式系统](03-distributed-systems/_index.md) → [4 数据系统](04-data-systems/_index.md) → [5 数据架构](05-data-architecture/_index.md) | 如何在分区、复制、存储和数据生命周期中保持正确性并可恢复？ |
| 生产与智能 | [6 云原生/SRE](06-cloud-native-sre/_index.md) → [7 ML 系统](07-ml-systems/_index.md) → [8 推荐系统](08-recommendation-systems/_index.md) → [9 AI 工程](09-ai-engineering/_index.md) | 如何把系统部署、运营、模型、推荐和 AI 能力放进可观测、可回滚、可控成本的生产边界？ |
~~~

Follow with the main route 00 → 01 → 02 → 03 → 04 → 05 → 06 → 07 → 08 → 09 and say that its macro order is stable.

- [ ] **Step 3: Add reading modes and four-layer method**

Add:

~~~
## 两种阅读方式

**主干阅读**按十个阶段依次推进。每个阶段先理解问题，再看组件怎样承载思想，最后留下一个可复核的项目产物。

**第一条纵向切片**只追踪一条因果链：[Page Cache、写入确认与持久化边界](01-computer-systems/page-cache-and-durable-io.md) → [Write-Ahead Log](04-data-systems/write-ahead-log.md) → [Replicated Log](03-distributed-systems/replicated-log.md) → [Checkpoint 与 Replay](05-data-architecture/checkpoint-and-replay.md) → [有状态服务恢复](06-cloud-native-sre/stateful-recovery.md) → [综合评审](capstone/log-state-recovery-review.md)。它用同一条“日志—状态—恢复”问题穿过多个阶段，不改变主干顺序。

## 四层学习法

1. **底层思想**：用状态、边界、时序和故障模型解释“为什么会这样”。
2. **组件设计落地**：把思想映射到一个组件或协议，并说明它新增了什么保证、又留下什么缺口。
3. **生产实践与真实案例**：沿着时间线看正常路径、故障现象、证据、处置和预防。
4. **动手验证与架构判断**：在隔离环境中做最小实验，再回答“何时使用、何时不使用、证据是否足够”。
~~~

- [ ] **Step 4: Add completion and material boundaries**

Add 如何判断完成 with a reading-versus-mastery distinction. Retain links to the capstone, validation protocol, authoring guide, and source matrix under explicitly labeled sections. Run npm run check:system-architecture; root markers should pass while phase markers may still fail.

- [ ] **Step 5: Commit the two index pages**

Run git diff --check and commit:

~~~
git add interests/_index.md interests/system-architecture/_index.md
git commit -m "docs(architecture): Add route map and interest entry"
~~~

### Task 4: Rewrite phases 0–4 as explanatory learning gates

**Files:**
- Modify: interests/system-architecture/00-architecture-method/_index.md
- Modify: interests/system-architecture/01-computer-systems/_index.md
- Modify: interests/system-architecture/02-network/_index.md
- Modify: interests/system-architecture/03-distributed-systems/_index.md
- Modify: interests/system-architecture/04-data-systems/_index.md
- Test: npm run check:system-architecture and npm run check:routes

**Interfaces:**
- Consumes: root map and existing formal-unit links.
- Produces: five phase pages with the exact seven learner headings and Chinese-first explanations.

- [ ] **Step 1: Rewrite phase 0 with a baseline artifact**

Use 阶段目标, 进入条件, 这一阶段先把什么讲清楚, 组件如何承载这些思想, 综合项目产物, 阶段挑战, and 阅读顺序. Explain that a quality attribute is a measurable constraint, a capacity estimate turns traffic into resource pressure, and an ADR records a decision and rejected alternatives. Require a request/constraint sheet, an order-of-magnitude estimate, and an initial architecture review.

- [ ] **Step 2: Rewrite phase 1 and preserve the Page Cache unit link**

Explain process/thread, virtual memory/page fault, file descriptor, page cache/writeback, buffering, and backpressure as one causal chain. For every concept, state what the operating system is doing and what an architect can observe. Introduce Linux and JVM in Chinese-first form, then keep Netty/Kafka/database names as examples. Link page-cache-and-durable-io.md in 阅读顺序 and require a timeline from application write to durable/recoverable confirmation.

- [ ] **Step 3: Rewrite phase 2 around partial failure**

Explain connection state, timeout, retry, queueing, tail latency, and rate limiting in request order. Define 部分失败 as one participant progressing while another is delayed or unreachable. Ask the reader to calculate how retries change offered load and to identify evidence distinguishing server slowness from network loss.

- [ ] **Step 4: Rewrite phases 3 and 4 around state and storage choices**

For Distributed Systems, explain partition, replication, leader/follower, quorum, commit, and election before naming Kafka/etcd. Keep the existing vertical-slice link order and explicitly distinguish main route from the temporary WAL detour. For Data Systems, explain row/column storage, B+ tree/LSM tree, WAL, MVCC, buffer pool, and query execution as answers to different access patterns; link the WAL unit and state the expected evidence from a storage decision.

- [ ] **Step 5: Run focused checks and commit phases 0–4**

Run npm run check:system-architecture, npm run check:routes, and git diff --check. Commit:

~~~
git add interests/system-architecture/00-architecture-method/_index.md interests/system-architecture/01-computer-systems/_index.md interests/system-architecture/02-network/_index.md interests/system-architecture/03-distributed-systems/_index.md interests/system-architecture/04-data-systems/_index.md
git commit -m "docs(architecture): Explain foundational learning gates"
~~~

### Task 5: Rewrite phases 5–9 and the capstone index

**Files:**
- Modify: interests/system-architecture/05-data-architecture/_index.md
- Modify: interests/system-architecture/06-cloud-native-sre/_index.md
- Modify: interests/system-architecture/07-ml-systems/_index.md
- Modify: interests/system-architecture/08-recommendation-systems/_index.md
- Modify: interests/system-architecture/09-ai-engineering/_index.md
- Modify: interests/system-architecture/capstone/_index.md
- Test: npm run check:system-architecture and npm run check:routes

**Interfaces:**
- Consumes: phase 0–4 terminology and the root map.
- Produces: explanatory production/AI phase gates and a capstone index that is visibly the end of the route.

- [ ] **Step 1: Rewrite phase 5 around data lifecycle and replay**

Explain event time, processing time, window, watermark, late event, state, checkpoint, and replay with one late-event example. Put Kafka/Flink/Spark/Iceberg/warehouse names after the lifecycle explanation. Link checkpoint-and-replay.md and require a schema/data-quality/replay evidence packet.

- [ ] **Step 2: Rewrite phase 6 around operating a stateful system**

Explain container isolation, desired state/reconciliation, stable identity, persistent volume, SLI/SLO, error budget, RPO/RTO, release, rollback, and incident review in Chinese. Link stateful-recovery.md and require a deployment diagram, SLO table, alert runbook, and recovery rehearsal record.

- [ ] **Step 3: Rewrite phases 7–9 as a dependency chain**

For ML Systems, explain features/labels, train/validation/test split, leakage, serving, versioning, drift, and rollback. For Recommendation Systems, explain candidate generation, ranking, re-ranking, feedback loop, cold start, and experiment bias. For AI Engineering, explain token/context, embedding/retrieval, RAG, workflow/state machine, agent loop, evaluation, guardrails, and tool permissions. Each page must state what it inherits from the previous phase and what it does not promise.

- [ ] **Step 4: Make the capstone index a completion gate**

Retain privacy and cleanup rules. Add a 阶段位置 paragraph linking to the course root and an ordered table mapping the six formal units to the evidence consumed by the final review. Explain that the capstone proves judgment under constraints; it is not a new seventh theory layer. Link the validation protocol as a separate evidence document.

- [ ] **Step 5: Run focused checks and commit phases 5–9**

Run npm run check:system-architecture, npm run check:routes, and git diff --check. Commit:

~~~
git add interests/system-architecture/05-data-architecture/_index.md interests/system-architecture/06-cloud-native-sre/_index.md interests/system-architecture/07-ml-systems/_index.md interests/system-architecture/08-recommendation-systems/_index.md interests/system-architecture/09-ai-engineering/_index.md interests/system-architecture/capstone/_index.md
git commit -m "docs(architecture): Connect production and AI phases"
~~~

### Task 6: Orient the first three formal units for Chinese-first reading

**Files:**
- Modify: interests/system-architecture/01-computer-systems/page-cache-and-durable-io.md
- Modify: interests/system-architecture/04-data-systems/write-ahead-log.md
- Modify: interests/system-architecture/03-distributed-systems/replicated-log.md
- Test: npm run check:system-architecture, npm run check:routes, and generated HTML inspection

**Interfaces:**
- Consumes: exact existing frontmatter and four-layer headings.
- Produces: five orientation headings, glossary tables, and explicit vertical-slice navigation for the first three formal units.

- [ ] **Step 1: Add orientation blocks**

Immediately after each H1 add the headings 一句话理解, 本页要解决的问题, 本页词汇, 在路线中的位置. Each unit gets a state-specific summary and a 6–10-row glossary. For example, the Page Cache unit begins:

~~~
## 一句话理解

一次写入会依次经过应用缓冲区、系统调用、页缓存、回写、设备缓存和稳定介质；“调用成功”只说明其中一个边界被跨过。

## 本页要解决的问题

当事件接入服务在 write() 返回后就回复成功，机器随后异常重启时，哪些事件可以被证明已经满足业务确认？

## 本页词汇

| 中文 | English | 在本页中的含义 |
|---|---|---|
| 页缓存 | Page Cache | 内核暂存文件页、合并读写的缓存层 |
| 脏页 | Dirty Page | 内存内容已更新但尚未完成后续写回的页 |
| 写回 | Writeback | 把脏页推进到文件系统和块设备的过程 |
| 稳定介质 | Stable Storage | 在当前故障模型下可恢复的数据边界 |

## 在路线中的位置

这是纵向切片起点，先修是[架构方法与基线诊断](../00-architecture-method/_index.md)，下一步是[Write-Ahead Log](../04-data-systems/write-ahead-log.md)，完整地图见[系统架构与 AI 工程](../_index.md)。
~~~

Use the same headings for WAL and Replicated Log with their own state transition and 6–10 terms. Add ## 下一步 after 权威来源与延伸阅读. Link WAL → Replicated Log → Checkpoint as appropriate.

- [ ] **Step 2: Rewrite first-use terminology and layer introductions**

Use Chinese-first forms such as 预写日志（Write-Ahead Log，WAL）, 复制日志（Replicated Log）, and 提交索引（commit index）. Keep exact Kafka/SQL/API/config names in code style. Add one sentence before each four-layer section explaining what the reader should be able to answer after that layer.

- [ ] **Step 3: Verify and commit**

Run npm run check:system-architecture, npm run check:routes, and git diff --check. Inspect generated HTML to confirm the orientation block appears before the learning goal. Commit:

~~~
git add interests/system-architecture/01-computer-systems/page-cache-and-durable-io.md interests/system-architecture/04-data-systems/write-ahead-log.md interests/system-architecture/03-distributed-systems/replicated-log.md
git commit -m "docs(architecture): Orient the first recovery slice"
~~~

### Task 7: Orient the remaining formal units and final review

**Files:**
- Modify: interests/system-architecture/05-data-architecture/checkpoint-and-replay.md
- Modify: interests/system-architecture/06-cloud-native-sre/stateful-recovery.md
- Modify: interests/system-architecture/capstone/log-state-recovery-review.md
- Test: npm run check:system-architecture, npm run check:routes, and generated HTML inspection

**Interfaces:**
- Consumes: first-slice link vocabulary and orientation contract from Task 6.
- Produces: the final three units with the same learner orientation and explicit slice continuation/termination.

- [ ] **Step 1: Add orientation blocks with unit-specific state transitions**

For Checkpoint/Replay, explain “有状态计算在故障后从哪个一致性边界继续”; for Stateful Recovery, explain “控制器重建与应用数据恢复不是同一个动作”; for the capstone, explain “把六页证据放进同一条故障时间线并做取舍”. Each glossary contains 6–10 terms and each page links its previous and next unit. Mark the capstone as the slice endpoint and link back to the course root.

- [ ] **Step 2: Add 下一步 sections and remove dangling English-only transitions**

Each section names the next main-route page, the next vertical-slice page or capstone, and one intentionally deferred topic. Use Chinese prose with code-styled official names only where precision matters.

- [ ] **Step 3: Run checks and commit**

Run npm run check:system-architecture, npm run check:routes, npm run check:breadcrumbs, and git diff --check. Commit:

~~~
git add interests/system-architecture/05-data-architecture/checkpoint-and-replay.md interests/system-architecture/06-cloud-native-sre/stateful-recovery.md interests/system-architecture/capstone/log-state-recovery-review.md
git commit -m "docs(architecture): Complete formal unit navigation"
~~~

### Task 8: Full verification, generated-page review, and PR update

**Files:**
- Modify: website/scripts/check-system-architecture.js only if a required assertion is missing
- Modify: PR #34 description through gh api after all content is verified
- Test: full website checks and generated-page smoke inspection

**Interfaces:**
- Consumes: all site and content changes from Tasks 1–7.
- Produces: a verified branch head and PR description recording the revised entrance, map, language policy, and manual-validation status.

- [ ] **Step 1: Run the complete command set**

From website/ run:

~~~
npm run check:system-architecture
npm run check:routes
npm run check:breadcrumbs
npm run build
~~~

From the worktree root run:

~~~
git diff --check
git status --short
~~~

Expected: all four website commands exit 0; build reports generated pages without route errors; status lists only intended tracked changes plus preserved unrelated derby.log/logs/ files.

- [ ] **Step 2: Inspect the generated navigation path**

Use rg against website/site/index.html, website/site/interests/index.html, website/site/interests/system-architecture/index.html, and one formal-unit output to verify:

~~~
兴趣副线 → 系统架构与 AI 工程 → 一张图看懂路线 → 正式单元 → 下一步
~~~

Confirm every generated HTML page contains the shared top-nav interest link and every formal unit contains the five orientation headings in order.

- [ ] **Step 3: Update PR #34 description**

Use gh api repos/JohnnyChenS/gulou/pulls/34 --method PATCH with a body that preserves the original scope and verification notes and adds:

~~~
Review revision:
- Added interest-track entry points to shared navigation and the dynamic homepage.
- Rebuilt the course root as a three-arc map with main-route and vertical-slice reading modes.
- Rewrote phase indexes as explanatory learning gates and added Chinese-first orientation/glossaries to formal units.
- Added static checks for the learner-facing navigation contract.
~~~

Do not claim human validation is complete; keep the PR in Draft until the reader can perform the four manual acceptance checks from the spec.

- [ ] **Step 4: Commit only final checker corrections and report evidence**

If verification finds a missing assertion, make the smallest apply_patch correction, rerun the complete command set, and commit it with test(architecture): Tighten navigation regression check. Do not alter unrelated files. Report the final branch commit, command results, generated-page paths, and remaining human-validation items.
